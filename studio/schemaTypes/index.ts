import { post } from './post';
import { video } from './video';
import { evento } from './evento';

// Todos os tipos de conteúdo que aparecem no painel do Sanity.
export const schemaTypes = [post, video, evento];
